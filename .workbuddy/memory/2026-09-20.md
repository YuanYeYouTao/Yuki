# 2026-09-20

## system_prompt.md 具体示例填充

- 目标位置：`config/system_prompt.md` 第 78 行起「具体示例（仅供参考）」，原先为空，现已填入 5 段对话示例。
- 角色分配（用户随后纠正）：**不能固定按人映射**。日常段 Yuki = 小尤；
  **工作模式段 Yuki = 亦难**（亦难才是那个给得出一手建议的人）。
  另一侧在示例里统一写作「用户」，即 Yuki 的对话对象，不按原记录里的真实身份。
  → 通用规则：哪一侧的语气是该场景下想让模型输出的，那一侧就标 Yuki，逐段判断。
- 格式：`场景：xxx` 起头，逐行 `Yuki:` / `用户:`，场景之间不空行。
- 5 个场景：拉肚子 / 催睡 / 心疼 / 没回消息 / 买电脑（工作模式）。
- 待确认点：
  - "真的公式"疑似"真的够用"错字，保留了原文（已提示用户）。
  - 第三、四段的同理切换用户未要求，保持小尤 = Yuki。
- `config/system_prompt.md` 被 .gitignore:29 排除，不入库，聊天正文不会进公开仓库。
- 生效提醒：system prompt 只在进程启动时加载，本地改完需同步到宿主机
  `/opt/yuki-qqbot/config/system_prompt.md` 再 `docker restart qq-ai-bot-bot-1`。

## 已同步上线（02:18-02:22）

- 备份 `system_prompt.md.bak.20260920`（9620 字节，旧版），scp 推本地版（7197 字节 / 195 行），
  `docker restart qq-ai-bot-bot-1`。
- 校验走代码路径：`Settings().system_prompt` → LEN=3082，含"场景：拉肚子""场景：买电脑"，
  旧版"禁止的表达示例"已不在。生效确认。
- **本地版比旧生产版小 2.4KB**：旧版多出 15 字硬限制、"鼓励用空格代替标点"、宽容性条目、
  6 个短语库词、以及一整段"正确/错误格式示例 + 禁止的表达示例"。就此问过用户，
  答复是"以我发的为准"→ 本地版权威，旧版这些段落作废，不要再按 diff 回滚。
