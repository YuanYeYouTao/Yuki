# 子 Agent 定向验收记录

日期：2026-09-13。模型保持生产配置 `deepseek-flash`，协议 Responses，外部联网不变。

## 请求协议

19 项子任务验证覆盖真实 Runner、调度器、SQLite 和最终 HTTP 序列化。
三组适配器为 Chat Completions、DeepSeek Responses、含原生工具的 OpenAI Responses。
普通续跑的实际 `messages/input` 旧前缀完整保留率 100%；完整工具数组不漂移。
覆盖 24 次模型请求分段、32 次业务调用分段、父子提问、显式压缩、
图片恢复、取消失效、共享预算并发竞争、完成通知修复和七天归档。
另验证图片发布检查、视觉请求计费、超大输入拒绝，以及 0057 新建数据库迁移和启动校验。

## 实际 Flash 缓存

隔离进程从生产数据库只读备份加载配置和 MCP 声明，不启动 Bot、QQ 发送或后台业务任务。
采用完整 36 项工作者工具和完整 174 项主 Agent 工具，使用真实预算实现作代码评审资料，
没有填充无关文字。主 Agent 对照使用配置中的人格提示词和相同评审目标；
这是受控 API 对照，不是生产群聊流量的总体统计。

每条链包含首次请求、连续执行、父任务补充、休眠 12 秒后恢复、总结、显式压缩后请求。
比较发送前的实际 input 和完整 tools。普通请求只追加；压缩明确建立新链。
续跑时上次响应也成为可复用历史，新增问题小于输入的 5%。

| 样本 | 工作子 Agent | 主 Agent |
|---|---:|---:|
| 连续执行 | 8192 / 8349 = 98.12% | 43520 / 43702 = 99.58% |
| 父任务补充 | 9600 / 9778 = 98.18% | 44416 / 44551 = 99.70% |
| 短暂休眠 | 10752 / 10920 = 98.46% | 44544 / 44715 = 99.62% |
| 上述暖链合计 | **98.268%** | **99.633%** |
| 暖链未命中 tokens | 503 | 488 |
| 压缩后 | 7424 / 7680 = 96.67% | 43136 / 43331 = 99.55% |
| 六次请求全部合计 | 54400 / 55573 = 97.889% | 236800 / 264541 = 89.514% |
| 六次请求未命中 tokens | 1173 | 27741 |

工作者暖链比对照低 1.365 个百分点，达到 95% 目标，没有触发低于 90% 或落后超过 5 个百分点的门槛。
上表工作者首次请求已被之前审计暖过（7424 / 7587）；不能当作冷启动结果。
更早的首次工作者启动实测 0 / 7587，其后连续、父任务补充和休眠分别为
7936 / 8182、10112 / 10254、11520 / 11670。这四次整体 29568 / 37693 = 78.44%，
未命中 8125 tokens。其中第二次因输出预算耗尽为 incomplete，仍保留原始返回项继续；
后续完整声明对照均为 completed。

主 Agent 六次整体较低来自首次请求（16384 / 43248），没有把首次请求排除后冒称整体命中。
压缩后仍命中固定合同及初始资料前缀。长时间闲置后的 Provider 淘汰未模拟；
不能由 12 秒休眠或这些样本承诺数小时后的命中率。

## 发布状态

本记录描述本地定向验证与受控在线 API 审计。CI、PR 合并和部署状态以实际发布记录为准；
不能凭本文件认定新版已经上线。
