# Memory 与查证可靠性：真实验收进度

本文件是未完成验收的检查点，不是发布通过报告。当前合同见
[收口任务书](Yuki-记忆与查证可靠性收口任务书.md)。版本 3.8.1、schema 0051 不变。

## 已定位并处理的实现问题

- 人物工具的群限定说明明确为默认省略；处于群聊或 @ 成员本身不是限制到该群。
- 指定群范围拒绝保留 `permission_denied`、`retryable=false`，补充范围和未执行查询标记。
  不自动扩大查询，不改变历史共同群权限，不输出未授权目标信息。
- overview 原有占位词法分数被错误投影为词法命中：删除占位值，总览匹配状态改为未知。
- 自省字段语义说明原被 compact schema 删除：保留字段说明，明确持久事实与单次经历边界。
- 结构化长度修复加入允许上限、实际数量等数值诊断；不输出任意校验 context。
- 自省证据白名单仅包含实际渲染的事件；超过输入预算而未展示的事件不能被引用。
  非法引用最多定向修复一次，仍不合法则失败，不进入 mutation。
- 历史 Episode 从 `self_facts` 分入 `existing_episodes`，携带真实 kind；保留原有正文、
  记忆引用和完整上下文，不把历史流水账当作本轮写作模板或新证据。
- 启动激活 restart override 时重新构造并校验 Settings，不能用保留分域缓存的
  `model_copy()`；否则客户端可能仍拿到更新前的模型、超时或其他长期配置。
  组合配置校验失败必须发生在创建客户端之前，错误不附带原始配置输入。

## 真实验证：失败记录全部保留

原始输入、完整 Main Agent 请求、工具参数与输出、冻结数据库副本仅存仓库外受限目录。
没有写入生产记忆或发送真实 QQ 消息，也没有清空上下文。

| 案例 | 实际观察 | 判定 |
|---|---|---|
| I02-r3 | 使用正确事件引用，但自行增加群限定；被拒绝后泛化为整个人无权限 | 失败 |
| I02-r4 | 省略群限定，正确人物查询返回有界结果，回复承认未列完；仍改用兼容账号入口 | 有改善，引用选择合同未完全通过 |
| reflection-r2 | 两批：一批修复后仍证据超限而失败；另一批完成但混合无关话题 | 失败 |
| reflection-r3 | 两批完成，实际提交三条；定向修复成功，但仍有叙事 fact 和混杂 Episode | 结构通过，语义失败 |
| reflection-r4 | 两批首次输出均通过结构校验，实际提交两条 Episode；没有新叙事 fact，但群 Episode 仍混杂话题 | 部分改善，整体未通过 |
| reflection-r5 | 实验把价值与证据字段放到正文前，仍产生混杂群 Episode，私聊日期叙述也需核对 | 未通过；未保留该字段重排实验 |
| reflection-r6 | 区分历史 Episode 后，两批完成并提交两条 Episode，但群内容仍混杂 | 语义未通过 |
| reflection-r7 | 仅私有验证启用 low 思考；两批提交三条，话题更集中；一次输出超限后修复，仍有叙事 fact 和缺依据的首次经历措辞 | 未通过，未改变生产思考配置 |
| dream-r1 | 两个冻结来源均返回 keep，混杂旧 Episode 未整理 | 机制完成，不代表效果通过 |
| dream-r2 | 同一来源仅切换 low 对照，三次请求完成两次 recompose，提交三条 Episode；一次缺来源字段经修复通过 | 局部改善，不代表完整 Dream 验收 |
| I08-r2 | 按名称查询不存在的对象，得到不可重试 not_found，未猜成发言者、未重复查询 | 此受控案例通过，不替代歧义场景 |
| I07-r3 | 三次不同查询（两次 hybrid、一次 lexical），未把无关候选编成摄影经历；仍使用“一条没有”等过强缺失措辞 | 查询选择改善，结果表达未完全通过 |
| I01-r2 | 私有验证误用空的 legacy Flash model 字段检查显式 Profile，尚未发送请求即中止 | 验证前置失败，不计调用；修正私有检查，不改用户配置 |
| I01-r3 | 完整 87 条消息、固定 8 工具；两次 low 请求。名称 overview 返回身份与别名，另一次本人相关查询；仍断言其他事没有存档 | 结果表达失败，保留原记录 |
| I01-r4 | 完整 87 条消息、固定 8 工具、low；没有调用读取工具，要求用户解释已给出的人名 | 查询选择失败；未走工具结果路径，不能证明或否定 grounding 传递修复效果 |
| I01-r5 | 完整 87 条消息、固定 8 工具、两次 low；姓名人物查询和群查询，实际模型输入保留 grounding 与非穷尽标记；回复保留争议限定并声明不保证全部存档 | 此受控案例通过，不代表独立验收或总体稳定性 |
| attribution-r1 | 隔离 Profile 替换因 Settings 的缓存分域未更新而失败；尚未发送模型请求 | 验证前置失败，不计调用；原记录保留 |
| attribution-r2 | 使用 I07-r3 实际进入模型的工具结果与已发送回复；六条全部评估，恰好标记复述的两条事实为使用 | 该机制案例通过，不代表线上总体使用率 |

这些是同一冻结 corpus 的受控复测，不是独立验收集，也不是还原线上历史状态的指标。
单次成功不能证明模型已稳定遵守合同；批次完成不能代替实际提交内容的质量判断。
私有验证脚本曾误把 `process_once()` 返回的整数当结构化对象，导致结果摘要未写出；
对应数据库和原始请求保留。现改用 `run_now()` 的结构化结果，并以数据库核对实际结果，
不将该脚本报告错误误归为生产自省故障。

Dream-r2 的只读数据库审计确认：两条来源仅修改 status/updated_at，原文未删；三条新
Episode 长度为 223/203/355 字，归属与来源一致，操作到结果的关联完整。聊天、证据、
Conversation、Presence 和三路由表摘要不变，integrity/FK 通过。原来源没有逐条 evidence，
故只能确认来源记忆及操作映射，不能宣称其历史主张获得独立查证。该探测手工选簇且未接
embedding scheduler，不能证明自动规划或提交后的 embedding 入队；此限制必须保留。

模型参数对照发现 structured runner 固定禁用思考；当前 flash profile 也显式禁用。
low 对照只在隔离客户端进行，没有修改用户 profile、人格或线上配置。自省 2400 tokens
预算在一次 low 请求中耗尽，不能仅根据话题改善就直接上线启用思考。

后续用户明确要求所有生成模型至少 low。实现与配置迁移边界由
[最低思考合同](model-reasoning-policy.md) 跟踪；这一决定取代禁用思考的运行策略，
不把上述失败案例改判为通过，也不放宽真实验收或请求预算。

attribution-r2 通过实际任务执行层发送 low/4096 请求，没有末端参数拦截改写。
只在私有 Profile 中为此前已经验证 low 的 DeepSeek Flash 补充能力声明，用户配置未改。
该例的摄影答案未获得目标事实，但回复实际复述了另两条记忆，因此这两条仍应计为使用；
不能把“未回答目标问题”等同于“完全没用记忆”。其余四条候选判为已评估但未使用。
此前验证脚本的 Settings 复制方式已退出使用，改成重新校验并断言分域与顶层配置一致；
旧调用/失败记录和旧统计不覆盖、不补算。

## 剩余硬门槛

### 发布检查与冻结副本审计（后续离线检查）

当前代码完整回归 800/800（213.44 秒）。显式提供 `YUKI_VERSION=3.8.1` 后 Compose
config 校验通过，release identity、0051 schema/迁移合同、Plugin API 2.0、离线质量
19/19 与 38/38 检查通过。正式 tag/main 归属没有验证，也没有创建 tag 或发布镜像。
发布检查生成的报告留在私有验证目录，仓库内自动生成文件恢复原状，未改基线。

在冻结数据库的隔离只读副本执行生产质量审计：SQLite integrity=ok、FK 违规为零，
但现有审计规则报告 249 条 error 计数（不是 249 个已确认损坏对象，检查之间可重叠）：

- `contested_state_invalid` 26；`superseded_without_chain` 162。
- `evidence_source_event_missing` 1；`evidence_source_invalid` 53。
- `evidence_excerpt_missing` 3；`contradiction_state_mismatch` 4。
- 无 evidence 的 automatic/rebuild fact 1428 条属于 warning，不自动改成 error 或删事实。

53 条来源检查失败全部关联 SELF/agent_reflection，其中 52 条为 Yuki outbound。
现有检查统一要求 human inbound，与自省可使用 Yuki 输出的现役合同存在不匹配，必须
按实际生产验证规则进一步核对；不能直接把所有失败降级、忽略或宣布修复。替代链与
冲突状态检查也需核对 Dream/Mutation 的真实账本合同。本轮没有修库或改审计规则。
这不是上线前的实时生产审计；在检查规则与真实异常分清前，发布审计门仍未通过。

### 样本选择偏差复核

重新审计冻结副本的最近两个完整 Asia/Shanghai 自然日（2026-09-07、09-08）：

- 人物入站 message 共 874 条，分布在四个 canonical Conversation。
- 旧候选脚本先要求存在 trigger hash 匹配的召回回执，再限制正文 7–600 字，
  最后取最近 180 条；这不是不依赖系统行为的自然问题样本。
- 完整窗口有 268 条正文不足 7 字，可能包含必须结合上下文理解的短指代；
  不能仅靠长度决定是否有查证机会。另有 80 条空白正文，需检查附件/segments。
- 240 条有 trigger hash 匹配、634 条没有。该匹配未证明 canonical Conversation
  一致；没有匹配也不能被解释为“模型没有查工具”。两项均不能直接作为查询率分母。
- 旧候选中只有 137 条落在上述完整窗口。窗口内另有 21 组同 Conversation 的精确
  规范化重复，共 48 条后续重复；这是人工审阅提示，不是自动语义去重或话题划分。

已在仓库外生成独立于回执的只读审阅清单，保留原候选与旧失败记录。资格、话题窗口、
查证机会、必要事实、校准/验收分组均保持未标注，不把 874 条计为合格样本。
原始来源哈希在导出前后相同；本次没有外部模型请求。下一步必须结合原有上下文、
真实目标和读取范围完成标注，避免以系统已经召回过的消息评价系统是否主动补查。

后续离线检查使用生产 `MemoryReadScopeResolver` 和 `MemoryFactRepository`，在隔离副本
连接上启用 query-only；没有重写社会关系权限规则。874 条输入涉及的 13 个发言者均与
有效 Binding 对应。对 1103 条非 SELF 事实逐条检查授权，其中 active 为 704 条；
授权矩阵有意保留旧状态以区分“允许读取”与“当前可检索”，不能直接充当检索候选池。
SELF、原始 evidence、写入以及历史时点的成员关系不在这一核验结果内。

一个此前已探测的人名身份问题，按真实名称入口可唯一解析，获准读取的 active 人物及
人物群事实为 114 条。人工检查已标出核心身份及别名的正例锚点，不能将同名闲聊都标为
身份答案。其余相关性标签尚未完成；该话题已用于开发探测，明确排除出独立验收集。
这证明当前冻结 corpus 下名称/社会权限路径可用，不证明历史执行或当前模型选择正确。
完整账本已为全部输入建立并校验定位，问题后的回答不作为其前置证据；账本本身也不
等于当时实际 Prompt/Rollup。上述产物全部留在受限私有目录，本次外部请求仍为零。

后续 I01 复测新增四次外发（Main Agent 两次、embedding 两次）。检查真实 Provider
请求发现：原始工具结果携带的 `memory_grounding_policy` 在统一结果归一化时丢失；
固定 Prompt 中规则仍在，但不能据此声称工具结果解释完整送达。当前修复仅允许核心
记忆读取工具保留该字段，并为三个列表工具补充有界、非穷尽标记及返回数量。
新增断言覆盖原始工具执行→归一化→结果预算后的模型载荷，插件同名字段不会被提升
为可信规则。尚未用真实模型复测修复后的回复，不宣称过强缺失结论已经消除。
本次最终回归 800/800（232.47 秒），Ruff format/check、mypy、diff 检查通过；离线
Memory quality 19/19 通过。完整事实前缀的预算同时计算归一化后的工具封装，防止规则
恢复后在下游被意外转换成摘要或 artifact。未新增测试项、迁移、权限或模型调用。

I01-r4 后审查发现旧 `available_memory_subjects` 仅是本条消息的引用绑定，却容易被读成
可查询人物名单。现改名为 `event_bound_memory_refs`，固定 entity 合同明确它不是权限
白名单，姓名仍由工具解析；不扩展自动预取、不新增前置模型。该歧义是否导致此次不查询
尚无法证明，改名也不构成模型行为验收通过。I01-r4 新增两次外发，其中一次 embedding。

I01-r5 再增加两次 Main Agent 与两次 query embedding 请求。已逐项核验实际下一次
模型请求中的规则、完整事实、有界字段及固定工具集合；本次回复没有重复“没有其他
存档”的错误。额外群查询也如实记录，不以工具调用更多作为通过理由。该案例已经多次
用于开发，绝不进入独立验收集。私有验证现保存精确 query 向量，键包括实际 Profile、
query instruct 和文本摘要；不与 document 编码混用，不改生产缓存，也不回填旧调用。

- I02 的真实引用选择、其余 Main Agent 与联网场景仍须完整复测。
- 自省单一核心经历、实际证据支持、无价值 noop 等语义验收尚未完成；Dream/attribution 未收口。
- 至少 60 合格真实样本、30 独立验收样本及阈值校准尚未完成，不能发布相关性通过率。
  原始候选中存在历史人工测试及相邻重复提问，不能直接算作独立自然聊天样本。
- 当前保留代码已通过 800 项 pytest、Ruff format/check、mypy；Memory quality 19/19、
  38/38 门禁和合同检查通过，未重设基线。冻结副本 integrity/FK 通过，SHA-256 未变。
  完整发布验证、真实质量门和部署前生产审计仍未完成；Docker smoke 未执行。
  只有全部硬门槛通过后才能本地构建并替换服务器 Bot。

本检查点累计实际外部请求 **94/96**：Main Agent 39、embedding 31、自省 18、Dream 5、attribution 1。
累计计数不重置；本文件不把后续请求额度提前视为已用，也不代表后续最终计数。
本检查点未推送、提 PR、发布或部署，线上仍未应用本分支。

## 后续授权：累计 144 次验证上限

用户随后授权增加不足的验证额度，并允许调整不合理的过紧约束。本轮采用此前提出的
累计 144 次上限，沿用原持久请求账本；授权时已用 94 次，剩余 50 次，不重置历史计数。
生成、embedding、修复、重试及真实联网外发仍逐次计数，预算耗尽前拒绝新的外发。
不合理的输出预算或旧审计规则需以实际调用、写入合同及回归测试证明后再调整；
这项授权不等于放宽读取权限、证据真实性、数据保护或独立真实效果验收门槛。
本节替代前述任务书的 96 次上限，既有探测及失败记录保持不变。

## SELF 事件证据审计纠偏

核对自省 mutation 与 repository 的真实写入合同后，确认 SELF 的 agent_reflection
证据允许引用 Yuki 的有效出站消息。旧发布审计与 provenance hygiene 扫描却只接受
人类入站，导致合法证据误报，甚至被清理预案列为无有效来源。二者现共用存量事件证据
谓词：SELF、reflection relation/authority、Yuki outbound、keeper 和非空正文同时
成立时才允许该例外。普通提取准入、人类事实证据及运行时授权均未放宽。

回归覆盖合法 SELF、普通 Person、非 reflection SELF、被抑制事件，以及清理扫描
不再选择合法 SELF；仅检查预案，没有执行清理。冻结副本只读复核后，事件证据错误
由 53 降为 1，全部审计错误计数由 249 降为 197，integrity/FK 仍正常。
其余错误并未豁免，包括缺失事件、excerpt、替代链和冲突状态；还需逐项鉴别。
工具 receipt 的证据生命周期也未由本次事件谓词修复证明完整，不宣称发布审计通过。
本次不消耗外部请求，不改事实、证据或账本数据。

随后检查工具回执生命周期：真实 mutation 写入的工具证据 event_id 本来为空，
旧审计却将其计入缺失事件；清理扫描也没有检查工具来源。现事件缺失检查仅针对
事件证据，工具证据另检查 receipt、触发事件、canonical 会话/所属范围、SELF
visibility、reflection authority/relation、来源账号和结果摘录。任一缺失或不匹配
仍报错；清理扫描复用同一规则，不能仅凭 receipt ID 认定证据有效。
已提交证据引用的 receipt 过期不影响来源有效性；原 cleanup 保留这些引用。
工具失败结果也可以作为失败经历的证据，不将 success 强制为 true。

真实 mutation 集成回归覆盖工具证据提交、过期保留、审计/清理扫描认可，以及
结果摘录不再匹配时拒绝。冻结副本错误计数进一步降至 196，工具来源检查为零；
其余错误保持原样，FK 为零。没有执行任何清理或修改冻结数据，仍未通过完整发布验收。

## 冲突状态两个维度

真实第三方纠正流程保留原事实 status=active，同时将 conflict_state 设为 contested；
新备选事实才使用 status=contested。读取投影保留 conflict_state，不能将 active 解释
为不存在争议。旧审计把 active+contested 一律报错，与 resolver/service 合同相反。
现仅删除这项错误判断；contested 状态必须带 contested 标记的检查与数据库约束保留。
回归通过真实 mutation 产生双状态，并验证非法清除备选冲突标记仍被数据库拒绝。

冻结副本因此减少 26 条误报，尚余 170 条审计错误计数。另四条 active 矛盾关系的双方
均有 contested 标记，但本次未修改其规则。162 条无替代链记录也全部缺少状态事件，
分属 Person 77、SELF 81、Group 3、PersonGroup 1；尚不能证明合法替代过程，
不得补造链条或删除记录来通过审计。此次没有模型调用、数据修改或部署。

## 历史链调查与 reflection-r8

冻结库中上述 162 条缺链事实的 updated_at 均在 2026-08-24 及以前；2026-08-25
及以后创建的事实逐日检查均有状态事件。这支持历史遗留的判断，但不能据此还原旧
事实的替代过程，也不是当前线上没有新缺陷的证明。审计错误仍保留，不补造历史。

继续检查现役维护路径发现 version_fact 在复制证据时遗漏 tool_receipt_id：已有
工具证据的 event_id 为 NULL，复制 DTO 会因缺少唯一来源而失败。现保留原工具
引用，并用真实 mutation 提交→过期回执保留→版本变更测试证明新版本保留证据，
旧版本正常 superseded。没有改变任何写入授权或为缺链历史数据执行修复。

reflection-r8 使用新隔离副本，显式输出预算 4096，通过真实执行层发送 low；
2 次请求、2 批完成、2 条实际 Episode 写入，均无修复与截断，副本 integrity/FK 正常。
逐条核对确认没有把日期叙事写进 fact/preference，自动化创建也有真实工具回执。
但私聊 Episode 混入无关关系互动，群 Episode 将历史消息中的外部说法复述为确定
分析，部分细节没有所选证据引用支持。原语义验收仍判失败，不修改预期。
完整请求、写入对照与人工判定保留在仓库外 reflection-r8，原失败探测不覆盖。
这仍是开发复测，不计独立验收样本，也未证明 noop。累计 **96/144**，剩余 48 次。

reflection-r9 修正自省任务说明中鼓励自由补充细节的表述，明确“记录讨论发生”不等于
“讨论中的说法已证实”，以及工具回执只能支持对应操作的实际结果。人格文件、模型
数量和结构化输出协议不变。固定合同通过实际模型请求构造测试，不能用该测试代替语义验收。
同一冻结副本再次运行两批，实际 low/4096、2 次调用、2 条 Episode，无修复或截断；
输出和落库正文逐条一致，integrity/FK 正常。无关的关系互动已消失，但仍有选中引用
不足以支持全部正文、无页面读取回执却称已看站点、复录未核实技术结论的问题。
原语义预期继续判失败，不能宣称提示说明已经修复生成行为。下一步需调查引用选择与
正文生成的脱节，不继续仅堆叠同义提示。请求累计 **98/144**，剩余 46 次；未部署。

reflection-r10 检查实际 schema 发现 Episode 先声明 content、最后才声明 evidence_refs。
现调整为引用、价值、重要性、正文；字段集合和限制不变，不加模型调用或新执行器。
实际请求 schema 与两次响应均确认引用出现在正文之前，但这只是生成顺序引导，
不是 JSON 协议保证，更不能作为语义校验。2 次 low/4096 请求完成两批并写入两条
Episode，副本正文与输出一致，integrity/FK 正常，51 项相关测试通过。
人工核对仍失败：有未被所选引用支持的答案、失败工具尝试及人物提醒细节。保持失败
记录，不把先列引用宣称为正确引用。累计 **100/144**，剩余 44 次；未部署。

reflection-r11 补齐后端输入来源元数据：事件/上下文增加 canonical author_kind；
已有 SELF fact/episode 增加 authority、conflict_state、evidence_count。候选没有的
元数据保持未知，不根据其正文推断为可信。固定说明明确作者为 Yuki 不构成独立核验，
active 不等于无争议，证据数量不证明语义正确。不增加读取范围或改变人格文件。
真实 low/4096 两次请求完成两批、写入两条 Episode，存储与输出一致，integrity/FK 正常。
语义依旧失败，且再次出现无依据“第一次”断言；人物来源元数据有用，但不能据此声称
模型已正确使用，引用选择与正文一致性仍未解决。累计 **102/144**，剩余 42 次。
本轮开发样本没有转入独立验收集；没有部署或生产写入。

## medium 单变量实验与文本口径核验

reflection-r12 仅在私有验证 catalog 克隆自省 Profile 并将该路由 effort 设为 medium；
模型、上下文、schema、4096 输出预算及其他任务路由不变，经真实 TaskModelExecutor
发送，没有在 Provider 末端改写请求。3 次请求（含一次修复），两批仅一批完成并写入
一条 Episode，另一批第一次 JSON 不完整、修复结果多出 result 包装而被严格拒绝。
首个响应 reported completion_tokens=4097、provider status=completed、reasoning_tokens
缺失，实际参数只有半截 JSON；不能单凭 provider completed 判成功，也不能确定其
思考/正文分别消耗多少。写入的一条仍有来源不充分的页面读取叙述，语义验收失败。
该实验不证明 medium 普遍更差，但不支持现在将线上自省直接改为 medium。
失败记录保留，线上 Profile 未改。累计 **105/144**，剩余 39 次。

独立只读核验三条 SQL 摘录异常：均是 SELF agent_reflection，原 excerpt 不在未经
处理的 chat_events.content 中，但全部存在于实际 renderer + normalize_memory_text
输出。其中一条正文为空、含 visual_summary；另外两条为非空正文规范化差异。
这证明旧 SQL substring 检查与实际写入口径不一致，不代表已修复全部相关检查；
后续需让审计与清理使用同一证据文本规则，不能简单跳过 excerpt 校验。

本次已将事件证据检查收口为共享校验器：普通来源仍按人类入站正文检查；SELF
reflection 按真实 renderer 及写入规范化后的文本检查，保留作者、speaker、keeper
与摘录真实性边界。删除被替代的 SQL 事件谓词；工具回执的独立来源检查保持不变。
审计以 100 行批次流式检查，报告最多保留 20 个样本 ID；清理按 ID 分页，避免前页
合法记忆遮住后续异常，待处理结果仍最多 500 条。未执行清理。
回归覆盖真实视觉摘要/规范化证据以及分页跨过合法 SELF 后仍发现异常；51 项相关测试
通过，分页变更后的治理 8 项另行通过，Ruff/mypy 通过。前一轮完整 800 项已通过
（308.59 秒），不将其冒充本次新接入代码的完整回归。
通过实际 audit.run 对 query-only 冻结副本复核，摘录/来源误报清零，剩余审计错误
166（替代链 162、active 矛盾关系 4）；schema0051、integrity/FK 正常。输出存入
私有 release-check-r3，旧报告未覆盖。未调用模型，累计仍为 **105/144**；未部署。
